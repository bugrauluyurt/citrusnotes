import React from 'react';

const Settings = (): JSX.Element => {
    return (
        <React.Fragment>
            <h1>Settings component</h1>
        </React.Fragment>
    );
};

export default Settings;
